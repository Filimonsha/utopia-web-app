import {useState} from "react";
import {getPersons} from "../../../api/family-management";

export const usePrepareListOfPersons = () =>{
    // TODO pagination
    const [data, setData] = useState([])

}
