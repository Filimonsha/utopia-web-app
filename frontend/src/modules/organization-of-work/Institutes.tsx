import {FC} from 'react';
import {Button} from "@mui/material";

interface IInstitutesProps {

}

const Institutes: FC<IInstitutesProps> = ({}) => {
    return (
        <div>
            <Button>Send report</Button>
        </div>
    );
};

export default Institutes;
